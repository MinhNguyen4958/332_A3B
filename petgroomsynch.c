#include <stdio.h>
#include <pthread.h>
#include "petgroomsynch.h"

int petgroom_init(int numstations) {
    // pet_t grooms[numstations];
    // for (int i = 0; i < numstations; i ++) {
    //     // Initially all grooming stations are available
    //     // grooms[i] = NULL;
    // }
    return 1;
}

int newpet(pet_t pet) {
    return 0;
}

int petdone(pet_t pet) {
    return 0;
}

int petgroom_done() {
    return 0;
}